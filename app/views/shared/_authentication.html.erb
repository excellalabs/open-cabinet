<% if user_signed_in? %>
  <%= link_to('sign out', destroy_user_session_path, :method => :delete, class: ("active" if current_page?(destroy_user_session_path))) %>
<% else %>
  <%= link_to('sign in', new_user_session_path, class: ("active" if current_page?(new_user_session_path)))  %>
<% end %>